# Root-owned runtime handoff for the unprivileged Network Monitor processor.
# /run is cleared on reboot; systemd-tmpfiles recreates this directory at boot.
d /run/config-integrity 0750 root nmgroup -
